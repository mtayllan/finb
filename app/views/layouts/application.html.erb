<!DOCTYPE html>
<html data-theme="night">
  <head>
    <title>Finb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5/theme/night.css" rel="stylesheet" type="text/css" />
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= favicon_link_tag asset_path("icon.png") %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <script src="https://cdn.jsdelivr.net/npm/@phosphor-icons/web"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet">
  </head>

  <body>
    <%= render "layouts/header" %>
    <%= render "layouts/bottom_navigation" %>
    <main class="container mx-auto mt-4 mb-20 md:mb-0 md:mt-14 px-5 flex pb-5">
      <%= yield %>
    </main>

    <% if notice = flash[:notice] || flash[:alert] %>
      <div tabindex="-1" role="alert" data-controller="element-removal" data-action="animationend->element-removal#remove" class="fixed bottom-0 z-100 flex max-h-screen w-full flex-col-reverse p-4 sm:right-0 sm:top-auto sm:flex-col md:max-w-[420px]">
        <div class="pointer-events-auto relative flex w-full items-center justify-between space-x-4 overflow-hidden rounded-md p-6 pr-8 shadow-lg border bg-base-100 text-base-content animate-slide-in-out" style="user-select: none; touch-action: none;">
          <div class="text-sm font-semibold <%= flash[:alert] ? 'text-warning' : 'text-success' %>"><%= notice %></div>
        </div>
      </div>
    <% end %>
  </body>
</html>
